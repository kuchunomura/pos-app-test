# POSレジ 取扱説明書

**バージョン**: 255（2026/05/12 時点）  
**対応端末**: iPad 第6世代 Safari / 一般ブラウザ  
**アプリURL**: https://kuchunomura.github.io/pos-app-test

---

## 目次

1. [基本画面構成](#1-基本画面構成)
2. [レジタブ（🛒レジ）](#2-レジタブ)
   - 商品エリア（左）
   - カートエリア（右）
   - 割引・予約サイト
   - 属性入力
   - 未登録商品
3. [支払い操作](#3-支払い操作)
4. [宿泊商品の操作](#4-宿泊商品の操作)
5. [保留機能](#5-保留機能)
6. [翌日・翌々日計上モード](#6-翌日翌々日計上モード)
7. [時間計算タブ（⏱時間）](#7-時間計算タブ)
8. [売上タブ（📊売上）](#8-売上タブ)
9. [設定タブ（⚙️設定）](#9-設定タブ)
10. [GAS連携の仕組みと注意点](#10-gas連携の仕組みと注意点)
11. [複数デバイス運用](#11-複数デバイス運用)
12. [アプリ更新・キャッシュ問題](#12-アプリ更新キャッシュ問題)
13. [データバックアップ・引き継ぎ](#13-データバックアップ引き継ぎ)
14. [よくある操作 Q&A](#14-よくある操作-qa)
---

## 1. 基本画面構成

画面上部に4つのタブがあります。

| タブ | 内容 |
|------|------|
| 🛒 レジ | 商品登録・カート・支払い（メイン操作） |
| ⏱ 時間 | 空中ウォーク・ツリーハウスの終了時刻計算 |
| 📊 売上 | 売上明細・集計・CSV出力・GAS同期 |
| ⚙️ 設定 | 商品・カテゴリー・各種設定 |

---

## 2. レジタブ

### 2-1. 商品エリア（左側）

#### カテゴリーフィルター

画面左上に横スクロールできるカテゴリータブがあります。

- **すべて**: 全カテゴリーの商品を表示
- 各カテゴリー名をタップ: そのカテゴリーの商品だけを表示

対応カテゴリー一覧:

| カテゴリー | 主な商品 |
|-----------|---------|
| 空中ウォーク | 55分・115分・1DAY |
| ツリーハウス昼 | 55分・115分・1DAY |
| BBQ | BBQスペース |
| 宿泊：ツリーハウス | ツリーハウス（人数別価格） |
| 宿泊：ドーム | ミラー・クリア・グレー |
| 宿泊：空中テント | テント①②③ |
| 宿泊：ハンモック | ハンモック①② |
| 備品レンタル | 焚火台・スキレット・ランタン等 |
| ペット | ペット料金 |
| 受付飲食 | コーヒー・アイスクリーム等 |
| グッズ | ポスカード・ひのきグッズ等 |
| 無人販売 | 薪・コーヒー等 |
| 消耗品 | マッチ・歯ブラシ等 |
| ピクニック飲食 | クッキー・タルト等 |

#### 商品ボタンをタップ

タップするとカートに追加されます。  
宿泊商品（ツリーハウス・ドーム・テント・ハンモック）は**人数選択モーダル**が開きます（→ [4. 宿泊商品](#4-宿泊商品の操作)）。

#### カテゴリーヘッダーの時間スロット表示

空中ウォーク・ツリーハウス昼のカテゴリーヘッダーには、**現在時刻からの終了時刻**がリアルタイムで表示されます。

- 例: `55分→14:35` `115分→15:35` `1DAY→17:00`
- カートに入っているコースは**太字・赤色**でハイライト
- 決済完了後3分間は直前の決済内容でハイライト継続

#### 翌日・翌々日ボタン（🌙 翌日 / 🌙🌙 翌々日）

チェックイン当日ではなく翌日・翌々日の売上として記録したい場合に使用します（→ [6. 翌日計上モード](#6-翌日翌々日計上モード)）。

#### ✏️ 編集ボタン

商品やカテゴリーの並び替え・個別編集が可能になる「編集モード」に切り替わります。

- 商品ボタン右上のドラッグハンドルで並び替え可能
- 商品ボタンをタップして編集・削除

---

### 2-2. カートエリア（右側）

#### 現在時刻・天気

右エリア上部に現在時刻がリアルタイム表示されます。  
時刻右の天気アイコン（🌤）をタップすると、今日の天気を複数選択できます（売上データに記録されます）。

#### 保留スロット

カート上部に10個の保留タブがあります（→ [5. 保留機能](#5-保留機能)）。  
データが入っているスロットはオレンジ色の点（●）で表示されます。

#### 前日繰越バッジ

前日に「繰越」メモ付きで記録された売上がある場合、カート上部に黄色バッジで表示されます。

#### カート操作

| 操作 | 方法 |
|------|------|
| 商品を増やす | カート内の `＋` ボタン |
| 商品を減らす | カート内の `－` ボタン（0になると削除） |
| 商品を削除 | 右端の `×` をタップ |
| カートをクリア | 下部の **🗑 クリア** ボタン |

#### 合計金額

カート下部に合計金額が大きく表示されます。  
割引適用中は元の金額が取り消し線で表示されます。

#### 支払いボタン

| ボタン | 支払方法 |
|--------|---------|
| 💴 現金 | 現金（お釣り計算あり） |
| 💳 カード | クレジットカード |
| 📱 電子 | 電子決済（PayPay等） |

---

### 2-3. 割引・予約サイト

左エリア下部の「**割引・予約サイト**」エリアをタップすると展開/折りたたみできます。

#### 割引ボタン（赤紫色でアクティブ）

| ボタン名 | 内容 |
|---------|------|
| 村内（半額） | レンタル以外の商品を半額。`free_mura:true` の商品（BBQ等）は無料 |
| 宿泊者 | 対象商品を宿泊者向け割引価格に変更 |

#### 予約サイトボタン（青色でアクティブ）

楽天・じゃらん・SOU・年間パス（昼）・年間パス（夜）など。  
選択すると宿泊商品に**「支払済」バッジ**が表示され、料金が ¥0 扱いになります。

> **注意**: 割引と予約サイトは同時に1つずつ選択可能です。同じボタンを再タップで解除できます。

---

### 2-4. 属性入力（👤）

左エリア下部の 👤 エリアをタップすると展開/折りたたみできます。  
顧客の年齢層・国籍を選択して売上データに記録できます（集計分析に活用）。

- 複数選択可
- 支払い完了後にリセット

---

### 2-5. 未登録商品（金額手入力）

左エリア最下部の入力欄に金額を入力して **追加** ボタンをタップすると、  
「未登録商品」としてカートに追加されます。

---

## 3. 支払い操作

### 現金払い

1. **💴 現金** ボタンをタップ
2. 決済確認モーダルが開く
3. 「受取金額」欄に受け取ったお金の金額を入力
   - 金額ボタン（50/100/500/1,000/5,000/10,000）をタップして積み上げ可能
   - **C** ボタンで入力をクリア
   - **ちょうど** ボタンで合計金額を自動入力
4. お釣りが自動計算・表示される
5. **決済完了** ボタンをタップ

### カード払い・電子決済

1. **💳 カード** または **📱 電子** ボタンをタップ
2. 確認モーダルが開く（金額のみ表示）
3. **決済完了** ボタンをタップ

### 決済完了画面

決済後に完了画面が表示されます。

| ボタン | 動作 |
|--------|------|
| ✅ 次のお客様へ | カートをクリアして次の会計へ（5秒後に自動実行） |
| 🔴 印をつけて閉じる | 売上に赤フラグを立てて閉じる（後で確認が必要な場合） |

---

## 4. 宿泊商品の操作

### 人数選択

ツリーハウス・ドーム・テント・ハンモックをタップすると**人数選択モーダル**が開きます。  
人数に応じて1人あたりの宿泊料金が変わります。

例（ツリーハウス）:
| 人数 | 価格 |
|------|------|
| 1名 | ¥14,000 |
| 2名 | ¥12,000 |
| 3名 | ¥10,000 |
| 4名 | ¥8,000 |
| 5名以上 | ¥8,000 |

> 各ドーム・テントには色付きの点（●）でタイプが識別できます（ミラー=灰、クリア=水色、グレー=グレー等）。

### 支払済みバッジ（楽天・じゃらん・SOU）

割引バーで **楽天 / じゃらん / SOU / 年間パス** を選択すると、  
宿泊商品に「支払済」バッジが表示され、料金が¥0になります。  
（バッジの文言・色は設定から変更可能）

---

## 5. 保留機能

複数グループの会計を同時進行させたいときに使います。最大10件まで保留できます。

### 保留する

1. カートに商品を入れた状態で **⏸ 保留** ボタンをタップ
2. タイトル入力モーダルが開く（空欄の場合は時刻がタイトルになる）
3. タイトルを入力して **保留する** をタップ
4. カートがクリアされ、保留スロットにデータが保存される

### 保留から戻す

1. 保留タブ（カート上部）の番号をタップ
2. そのスロットのカートが復元される

> 保留スロットにデータがある場合はタブにオレンジ点（●）が表示されます。

---

## 6. 翌日・翌々日計上モード

チェックインは今日でも、料金を翌日または翌々日の売上として記録したい場合に使用します。

### 使い方

1. 左エリア上部の **🌙 翌日** または **🌙🌙 翌々日** ボタンをタップ
2. カート右上に黄色バナー「🌙 翌日計上モード ON」が表示される
3. この状態で決済すると、売上日時が翌日（または翌々日）の日付で記録される
4. 同じボタンをもう一度タップするとモードが解除される

---

## 7. 時間計算タブ

施設の終了時刻を素早く計算するツールです。

### 単体計算

1. **施設**（空中ウォーク / ツリーハウス昼）を選択
2. **コース**（55分 / 115分 / 1DAY）を選択
3. **開始時間** を入力（または **今すぐ** ボタンで現在時刻を自動入力）
4. 終了時刻とカウントダウンが表示される

施設ごとの営業情報:

| 施設 | 営業時間 | 最終受付 |
|------|---------|---------|
| 空中ウォーク | 9:00〜17:00 | 16:00 |
| ツリーハウス昼 | 10:00〜16:00 | 15:00 |

### 両方利用（コンボ計算）

空中ウォークとツリーハウスを連続利用する場合の時刻計算です。

1. **順番**（空中ウォーク→ツリーハウス or ツリーハウス→空中ウォーク）を選択
2. 各施設のコース時間を選択
3. 開始時間を入力（または **今すぐ**）
4. 各施設の終了時刻が表示される

---

## 8. 売上タブ

### 明細ビュー（📋 明細）

| 操作 | 方法 |
|------|------|
| 今日の売上を表示 | **📅 今日** ボタン |
| 昨日の売上を表示 | **昨日** ボタン |
| 日付を絞り込む | 日付入力欄に範囲を入力して **絞込** ボタン |
| 全件表示 | **全件** ボタン |

明細上部のサマリーカード（売上合計・件数・人数・客単価）が表示されます。

#### 売上の編集・削除

明細テーブルの各行をタップ → **売上詳細モーダル** が開きます。

- メモの追加・変更
- 属性（年齢層・国籍）の変更
- 🔴 印の付与・解除
- 削除

#### 売上の修正（金額・商品変更）

明細テーブルの **修正** ボタンをタップすると、その売上データがカートに読み込まれます。  
修正後に再度決済することで売上データが更新されます。

---

### 集計ビュー（📊 集計）

期間を選んでグラフ表示できます。

| 期間ボタン | 内容 |
|-----------|------|
| 📅 今日 | 当日のみ |
| 今週 | 今週月〜日 |
| 今月 | 今月1日〜末日 |
| カスタム | 任意の開始〜終了日 |

グラフ種別:

| グラフ | 内容 |
|-------|------|
| 日別 | 日ごとの売上棒グラフ |
| 時間帯別 | 時間帯ごとの件数棒グラフ |
| 曜日別 | 曜日ごとの集計 |
| 商品別 | 商品別売上金額・数量 |
| 属性別 | 年齢層・国籍別の組数・人数 |
| 割引別 | 割引種別ごとの件数 |

---

### CSV出力

**📥 CSV** ボタンをタップすると、現在の絞込条件の売上データをCSVファイルとしてダウンロードできます。

---

### Googleスプレッドシート同期（📊 同期）

**📊 同期 ▾** ボタンをタップすると同期メニューが開きます。

| メニュー | 内容 |
|---------|------|
| 今日分 | 当日の売上データをGASへ送信 |
| 昨日分 | 前日の売上データをGASへ送信 |
| 絞込分 | 明細ビューの絞込条件分を送信 |
| 全件 | 全売上データを送信 |

> GAS WebアプリURLの設定は [設定タブ > スプレッドシート連携](#gas連携) で行います。

---

## 9. 設定タブ

### カテゴリー管理

既存カテゴリーの確認・削除、新規カテゴリーの追加ができます。

| 項目 | 説明 |
|------|------|
| カテゴリー名 | 商品グリッドに表示される名前 |
| 背景色 | 商品ボタンの背景色 |
| 枠色 | 商品ボタンの枠線色 |
| 文字色 | 商品名の文字色 |
| 金額色 | 価格の文字色 |

**保存**: 「カテゴリーを保存」ボタンをタップ。

---

### 商品登録

新商品の追加、既存商品の確認ができます。

| 項目 | 説明 |
|------|------|
| 商品名 | ボタンに表示される名前 |
| 金額（円） | 販売価格 |
| カテゴリー | 所属カテゴリー |
| 在庫数 | 0=在庫表示なし、1以上=残数表示あり |

> 商品ボタンの細かい設定（文字サイズ・名前カラー）はレジタブの「✏️ 編集モード」から商品をタップして編集できます。

---

### 割引・予約サイト設定

レジタブの割引バーに表示されるボタンを管理します。

| 種別 | 動作 |
|------|------|
| 予約サイト（支払済） | 宿泊商品を「支払済」扱いにして¥0表示 |
| 割引 | カート内の価格を割引計算 |

> 「村内（半額）」「宿泊者」は固定項目（削除不可）。  
> 楽天・じゃらん・SOU・年間パスは削除・追加が可能。

---

### 属性設定

年齢層・国籍の選択肢を管理します。  
デフォルトに戻したい場合は「デフォルトに戻す」ボタンをタップ。

---

### 時間計算設定

「時間」タブで使う施設・コースの設定（営業終了時刻・最終受付時刻・コース時間）を変更できます。

---

### 表示サイズ調整

| 設定 | 内容 |
|------|------|
| 文字サイズ | 70〜130%で調整 |
| カート幅 | 20〜45vw（ビューポート幅に対する比率）で調整 |

「保存」ボタンで設定を保存します。

---

### 支払済み表示設定

楽天・じゃらん・SOU選択時に宿泊商品に表示されるバッジの文言と色を変更できます。

- **バッジ文言**: 例「支払済」「事前決済」
- **バッジ色**: カラーピッカーで選択

---

### GAS連携（スプレッドシート連携）

Googleスプレッドシートへの自動同期設定です。

| 項目 | 説明 |
|------|------|
| GAS WebアプリURL | Google Apps ScriptのデプロイURL |
| 端末名 | スプレッドシートの「端末名」列に記録される名前（例：レジ1） |

**接続テスト**: URLを保存後、「🔗 接続テスト」ボタンで疎通確認できます。

---

## 10. GAS連携の仕組みと注意点

### 自動同期（決済ごと）

**決済が完了するたびに自動でGASに送信されます。** 手動操作は不要です。

| イベント | GASへの動作 |
|---------|-----------|
| 決済完了 | スプレッドシートに行を追加（`add_rows`） |
| 売上の修正（カートで再決済） | 元の行を削除 → 新しい行を追加（`replace_rows`） |
| 売上詳細でメモ・属性を変更して保存 | 該当行を更新（`replace_rows`） |
| 売上詳細から削除 | 該当行を削除（`delete_rows`） |

> 自動同期はバックグラウンドで行われます。失敗した場合はオレンジ色のトースト「⚠ GAS同期エラー」が表示されます。

### 手動同期（📊 同期ボタン）の用途

手動同期は以下の場面で使います：

- 通信エラーで自動同期が失敗したとき → 「今日分」で再送
- 別端末の売上をまとめてスプレッドシートに反映したいとき → 「全件」
- 特定日付の売上だけ再送したいとき → 日付絞込 → 「絞込分」

> **「全件」を選ぶと既存のシートをクリアして全件再送します。** 複数端末で運用している場合、一方の端末で「全件」を実行するとその端末が持っていない他端末の売上がシートから消えます（→ [11. 複数デバイス運用](#11-複数デバイス運用) 参照）。

### 電子決済時の自動メモ

電子決済（📱 電子）で決済すると、メモ欄に自動で以下が記録されます：

> **「電子決済、伝票2枚だからレジ締め注意」**

レジ締め時に現金との突合わせで混乱しないようにする注意書きです。売上詳細から削除できます。

### 前日繰越の取得

アプリ起動時と日付をまたいだタイミングに、GASから「繰越」メモ付き売上の合計を自動取得し、カート上部の黄色バッジに表示します。これにより複数端末で登録した繰越額が合算されて表示されます。

### GAS URLについて（TEST版）

現在のアプリは **TEST版** で、GAS URLはコードに固定されています（設定から変更しても起動時に上書きされます）。  
main版では設定タブで自由に変更できます。

---

## 11. 複数デバイス運用

### データの保存場所

このアプリのデータは各端末の **localStorage（ブラウザのローカルストレージ）** に保存されます。  
**デバイス間でデータは自動共有されません。**

```
端末A: localStorage（売上A）  →  GASスプレッドシート（売上A + 売上B が集約）
端末B: localStorage（売上B）  ↗
```

### 複数デバイスで運用する場合の原則

| 項目 | 挙動 |
|------|------|
| 売上データ | 各端末独立。決済時に自動でGASへ送信 |
| スプレッドシート | 全端末の売上が集約される（リアルタイム） |
| 商品・設定 | 各端末独立。変更は手動エクスポート→インポートで共有 |
| 保留データ | 各端末独立（端末間共有不可） |

### 端末名の設定（重要）

複数台で運用する場合、どの端末からの売上かをスプレッドシートで識別できるよう、**必ず端末名を設定してください**。

1. 設定タブ → **スプレッドシート連携** → 「端末名」欄
2. 例: `レジ1`、`レジ2`、`カウンター`
3. **保存** ボタンをタップ

設定した端末名はスプレッドシートの **P列（端末名）** に記録されます。

### 複数端末間で設定を統一する方法

1. 設定を整えた端末でデータをエクスポート
2. 他の端末でインポート
3. 売上は重複なしで追加、設定（商品・カテゴリー等）は上書き

### 「全件同期」の注意点

複数端末で運用中に一方の端末で **「全件」同期** を実行すると、その端末が知らない他端末の売上がスプレッドシートから消えます。

> **対策**: 全件同期は全端末のデータをエクスポート→統合インポートした端末から実行するか、「今日分」「昨日分」など部分同期を使ってください。

---

## 12. アプリ更新・キャッシュ問題

### アプリのバージョン確認

設定タブの最上部に現在のバージョン情報が表示されます。

```
POSレジ ver.255
更新: 2026/05/12 | TEST版
```

### アプリの更新方法

このアプリはGitHub Pages でホストされています。  
開発者が新しいバージョンをデプロイすると数分以内に公開URLが更新されますが、**ブラウザのキャッシュにより古いバージョンが表示され続けることがあります。**

#### 端末・ブラウザ別キャッシュクリア方法

---

##### iPad（Safari）

方法①：**ハードリフレッシュ（簡易）**
1. Safari のアドレスバーをタップ
2. URLを全選択して再入力し読み込む
3. ※キャッシュが残ることがあるため、解決しない場合は方法②へ

方法②：**Safariのキャッシュを完全削除**
1. iPad の「設定」アプリを開く
2. 「Safari」→「詳細」→「Webサイトデータ」
3. 「すべてのWebサイトデータを削除」をタップ
4. アプリを再度開く

> ⚠️ 方法②はlocalStorage（売上・商品・設定）も**すべて消えます**。必ず事前に **エクスポート** を取ってください。

方法③：**プライベートブラウズを使う（データ消去なし）**
1. Safari で新規プライベートタブを開く（右下の「タブ」→「プライベート」）
2. アプリURLを入力して開く
3. キャッシュを使わず最新版が読み込まれる
4. ただしlocalStorageが引き継がれないため、通常タブのデータは見えない（閲覧・確認専用）

---

##### PC（Chrome / Edge）

方法①：**ハードリフレッシュ（最もかんたん）**
- `Ctrl + Shift + R`（Windows）または `Cmd + Shift + R`（Mac）を押す
- キャッシュを無視して最新版を読み込む。**localStorageは消えません。**

方法②：**キャッシュのみ削除（localStorageを残したまま）**
1. アプリのページを開いた状態で `F12`（または右クリック → 「検証」）で開発者ツールを開く
2. 「Application」タブ（Chromeの場合）→ 左メニュー「Cache storage」→「Clear Site Data」ボタン
3. ※この方法ではlocalStorageは消えない

方法③：**ブラウザのキャッシュを完全削除**
- Chrome: アドレスバーに `chrome://settings/clearBrowserData` を入力 → 「キャッシュされた画像とファイル」にチェック → 「データを削除」
- Edge: `edge://settings/clearBrowserData` → 同様

> ⚠️ 方法③で「Cookieとサイトデータ」も削除した場合はlocalStorageも消えます。「キャッシュされた画像とファイル」のみならlocalStorageは残ります。

---

##### Lenovo タブレット（Android Chrome）

方法①：**ハードリフレッシュ（簡易）**
1. Chrome のアドレスバーをタップ
2. URLを再入力して読み込む（または更新ボタンを長押し → 「ページを更新」）

方法②：**Chromeのキャッシュを削除**
1. Chrome右上の「︙（メニュー）」→「設定」
2. 「プライバシーとセキュリティ」→「閲覧履歴データの削除」
3. 「キャッシュされた画像とファイル」にチェック（「Cookieとサイトデータ」はチェックしない）
4. 「データを削除」をタップ
5. ※この方法ではlocalStorageは消えません

方法③：**Cookieとキャッシュを両方削除（localStorageも消える）**
- 上記の手順で「Cookieとサイトデータ」もチェックして削除
- > ⚠️ localStorage（売上・設定）も消えます。事前にエクスポート必須。

方法④：**シークレットモードを使う（データ消去なし）**
1. Chrome右上「︙」→「新しいシークレットタブ」
2. アプリURLを開く → キャッシュを使わず最新版が読み込まれる

---

#### バージョンが更新されたか確認する方法

1. 設定タブ → バージョン情報の数字を確認
2. 開発者から通知されたバージョン番号と照合

### アプリ更新後にデータが消えた場合

localStorageが削除されると売上・商品・設定が消えます。

**復元手順:**
1. GASスプレッドシートには売上データが残っています（削除されません）
2. 以前エクスポートしたJSONファイルがあればインポートで設定・売上を復元できます
3. バックアップがない場合、商品・カテゴリーの設定は手動で再入力が必要です

> **定期的なエクスポートを強く推奨します。**（週1回程度）

---

## 13. データバックアップ・引き継ぎ

### エクスポート（バックアップ）

設定タブの「📤 全データをエクスポート」ボタンをタップすると、  
商品・カテゴリー・設定・売上・保留データをJSONファイルとして書き出します。

### インポート（端末引き継ぎ）

「📥 インポート（ファイルを選択）」ボタンをタップして、  
エクスポートしたJSONファイルを読み込みます。

- 売上・保留は**追加**（重複なし）
- 設定（商品・カテゴリー・GAS URL等）は**上書き**

### データリセット

特定の項目を初期値（コードのデフォルト値）に戻します。  
**手動で変更した設定は元に戻せません。**

| リセット対象 | 内容 |
|------------|------|
| カテゴリー | 宿泊4分割など最新の初期カテゴリーに戻す |
| 商品リスト | 最新の初期商品リストに戻す |
| 割引・予約サイト設定 | デフォルトの割引設定に戻す |

> **売上データ・GAS URLはリセット対象外**です。

---

## 14. よくある操作 Q&A

**Q. 決済後に間違えた金額で売上が記録されてしまった**  
A. 📊売上タブの明細から該当の行の **修正** ボタンをタップし、カートで修正後に再度決済してください。

**Q. 同じ商品を複数のお客様に同時会計したい**  
A. 保留機能を使います。1組目のカートを作って **⏸ 保留** → 2組目を入力 → 保留タブを切り替えて精算。

**Q. 天気を記録するには？**  
A. カート右上の天気アイコン（🌤）をタップして今日の天気を選択・保存すると、その日の売上に天気が記録されます。

**Q. 別のiPad（端末）に切り替えたい**  
A. 旧端末でデータをエクスポートし、新端末でインポートしてください。GAS URLも移行されます。

**Q. 売上の絞込で翌日のデータを見たい**  
A. 明細ビューの「明日」「明後日」ボタンで翌日・翌々日計上の売上データを確認できます。

**Q. カテゴリーやボタンを並び替えたい**  
A. レジタブの「✏️ 編集」ボタンで編集モードにすると、ドラッグ&ドロップで並び替えができます。

**Q. スプレッドシートに売上が反映されていない**  
A. 以下を順番に確認してください。  
①設定タブ → スプレッドシート連携 → 「🔗 接続テスト」で「✅ GAS接続OK」が出るか確認。  
②接続OKなら 📊同期 →「今日分」で手動再送。  
③「接続失敗」の場合はGAS URL、またはスプレッドシートの共有設定を確認。

**Q. 別の端末から見ると売上がない**  
A. 各端末のlocalStorageは独立しています。売上自体は決済時にGASに自動送信されているので、スプレッドシート側では全端末の売上を確認できます。POS画面で別端末の売上を見るには、その端末のデータをエクスポート → こちらの端末にインポートしてください。

**Q. アプリを開いたら古いバージョンのままだった**  
A. ブラウザのキャッシュに古いバージョンが残っています。端末別の対処：  
・**PC(Chrome/Edge)**: `Ctrl+Shift+R` でハードリフレッシュ（データは消えない）  
・**Lenovoタブレット(Chrome)**: Chrome「設定」→「閲覧履歴データの削除」→「キャッシュされた画像とファイル」のみチェックして削除（データは消えない）  
・**iPad(Safari)**: 「設定 → Safari → 詳細 → Webサイトデータを削除」→ **売上・設定も消えるため事前エクスポート必須**

**Q. 電子決済のメモに「伝票2枚...」と自動で入る**  
A. 電子決済で決済した際に自動付与される注意書きです。不要な場合は売上詳細のメモ欄から削除できます。

**Q. 「全件」同期したら他の端末の売上がスプレッドシートから消えた**  
A. 「全件」同期はシートをいったん全クリアして再送するため、その端末が持っていない他端末の売上は消えます。複数台運用時は「今日分」「昨日分」などの部分同期を使ってください。

---

## スプレッドシート（GAS）のシート構成

売上データはGoogleスプレッドシートに「M/D売上」（例：5/14売上）というシート名で記録されます。

| 列 | 内容 |
|----|------|
| A | 日時 |
| B | 売上合計 |
| C | 商品名 |
| D | カテゴリー |
| E | 数量 |
| F | 単価 |
| G | 小計 |
| H | 人数 |
| I | 割引 |
| J | 支払方法 |
| K | 年齢層 |
| L | 国籍 |
| M | 天気 |
| N | メモ |
| O | 売上ID |
| P | 端末名 |

シート1行目には集計（総件数・総売上・現金/クレジット/電子決済合計）が自動更新されます。  
2行目にはレジ現金の手入力欄と差額計算式があります。  
右側（R〜AC列）には商品別・年齢層別・国籍別の集計テーブルが自動生成されます。

---

*最終更新: 2026/05/14*

